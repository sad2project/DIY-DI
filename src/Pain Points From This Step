Pain Points to address in Step 7:

"Can a mock object return a mock that returns a mock?"

"Can you add some comments to this test code so I can tell what's going on?"

"If took me forever to refactor that class because of all the tests that needed
to expect the new contract"